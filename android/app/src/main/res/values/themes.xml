<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LinkMind" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.LinkMind" parent="Base.Theme.LinkMind" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Ensure splash_logo exists or use icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/icon</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.LinkMind</item>
    </style>
</resources>